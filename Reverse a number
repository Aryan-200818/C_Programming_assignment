#include <stdio.h>
int main(){
    int n,og,rev=0;
    printf("Enter a number: ");
    scanf("%d",&n);
    og=n;
    while(n>0){
        rev = (rev*10) + (n%10);
        n = n/10;
    }
    printf("%d in reverse will be: %d",og,rev);
    return 0;
}
