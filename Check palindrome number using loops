#include <stdio.h>
int main(){
    int n,og,rev=0;
    printf("Enter a number: ");
    scanf("%d",&n);
    og=n;
    while(n>0){
        rev = (rev*10) + (n%10);
        n = n/10;
    }
    if(og == rev) printf("%d is palindrome.",og);
    else printf("%d is not palindrome.",og);
    return 0;
}
