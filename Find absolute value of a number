#include <stdio.h>
int main(){
    int n;
    printf("Enter a number: ");
    scanf("%d",&n);
    if(n<0) {
        printf("Absolute value of %d: %d\n",n,-n); // -(-n) = n
    }
    else{
        printf("Absolute value of %d: %d\n",n,n);
    }
    return 0;
}
